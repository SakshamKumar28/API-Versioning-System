# API Versioning System - Frontend

A minimal React frontend to demonstrate the API Versioning System.

## 🛠️ Tech Stack
- **Framework**: React 19 (Vite)
- **Styling**: Tailwind CSS v4 (Custom Theme)
- **Theme**: Cream/Dark Green (Light) & Dark Green/Cream (Dark)

## 🚀 Getting Started

1. **Install Dependencies**
   ```bash
   npm install
   ```

2. **Run Development Server**
   ```bash
   npm run dev
   ```

## ✨ Features
- **Dual API Interaction**: Interact with both V1 and V2 backend APIs simultaneously.
- **Dark Mode**: Built-in toggle for Light/Dark themes.
- **Versioned Forms**:
  - **V1**: Create user with `Name` (Full String).
  - **V2**: Create user with `First Name` & `Last Name`.
- **Live Data**: View users from both API versions side-by-side.
